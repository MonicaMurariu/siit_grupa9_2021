 - Write a generic method to exchange the positions of two different elements in an array.

 Arraylist arr ={1, 2, 3, 4};
 x=arr[0];
 arr[0]=arr[1];
 arr[1]=x;

 arr= {2, 1, 3, 4}

 -  Write a generic method that finds the maximal element from a list

List l= {32, 11, 7, 54, 2}
    max = l[0];
    for (i=0; i<l.length; i++)
        if (max < l[i+1])
            max=l[i+1]
        else max
